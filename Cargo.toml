[package]
name = "countup"
version = "0.1.0"
authors = ["Archie <fruktoed@gmail.com"]

[target.'cfg(target_os="android")'.dependencies]
jni = { version = "0.5", default-features = false }

[lib]
crate-type = ["dylib"]

[dependencies]
